<div>
  <h1>Wikinimous</h1>
  <h2><%= link_to 'New article', new_article_path %></h2>
  <div>
    <% @articles.each do |article| %>
        <h3><%= link_to article.title, article_path(article) %></h2>
        <p><%= article.content %></p>
    <% end %>
  </div>
</div>
